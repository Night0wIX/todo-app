export const TODO_STATUS = {
  ACTIVE: "active",
  COMPLETED: "completed",
} as const;

export const FILTER = {
  ALL: "all",
} as const;

export const UNDO_DELAY_MS = 5000;
export const COMPLETION_ANIMATION_DELAY_MS = 300;
